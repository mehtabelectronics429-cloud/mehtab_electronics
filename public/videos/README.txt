Drop a looping background video here named "hero-bg.mp4" (H.264 MP4, ~1080p, muted,
ideally < 8 MB, 10-20s loop). It will appear as the cinematic full-screen backdrop
in dark mode. If absent, the site automatically uses the animated aurora fallback.
